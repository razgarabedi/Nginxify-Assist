# This is an example .env.local file.
# Copy this file to .env.local in the project root directory and replace the placeholder values
# with your actual credentials and keys.
# IMPORTANT: DO NOT commit your actual .env.local file to version control.

# --- SMTP Configuration (for Contact Form Email Sending) ---
# Replace these placeholder values with your actual SMTP server details.

# SMTP Server Hostname (e.g., smtp.gmail.com, smtp.office365.com)
SMTP_HOST=your_smtp_host

# SMTP Server Port (e.g., 587 for TLS, 465 for SSL, 25 for unencrypted)
# Port 587 (TLS/STARTTLS) is generally recommended.
# Port 465 uses SSL.
SMTP_PORT=587

# SMTP Username (usually your email address or a specific sending username)
SMTP_USER=your_smtp_username

# SMTP Password (your email password or, preferably, an app-specific password/API key)
# For services like Gmail, you often need to generate an "App Password".
SMTP_PASS=your_smtp_password


# --- Google GenAI API Key (for Genkit) ---
# Optional: Only required if you are using Genkit features that interact with Google AI models.
# Get your key from Google AI Studio: https://aistudio.google.com/app/apikey
GOOGLE_GENAI_API_KEY=your_google_genai_api_key


# --- Notes ---
# - After creating or modifying the .env.local file, you MUST restart your Next.js development server
#   (e.g., stop and restart `npm run dev`) for the changes to take effect.
# - Ensure the .env.local file is listed in your .gitignore file.
